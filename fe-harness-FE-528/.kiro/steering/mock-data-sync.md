---
inclusion: auto
---

# Mock Data Sync Reminder

Any change to mock JSON files, hardcoded mock data in components, new components that display mock data, or conditional visibility changes **MUST** also update `MOCK-DATA-GUIDE.md`.

The **Quick Scenarios** table in `MOCK-DATA-GUIDE.md` must be kept current — it is used for live demos to quickly show specific UI states.
